flowchart TB

%% ===================== USERS =====================
U[External Clients<br/>Users / Systems]

%% ===================== ACCESS LAYER =====================
subgraph ACCESS["Access & Control Layer"]
    GW[Tawk Gateway<br/>(A2A Endpoint)]
    AUTH[Authentication & Authorization]
    TRACE[Traceability / Audit]
    HEALTH[Health Check API]
end

U --> GW
GW --> AUTH
GW --> TRACE
GW --> HEALTH

%% ===================== PLATFORM CORE =====================
subgraph CORE["TAWK Platform Core"]
    ORCH[Orchestrator Agent]
    REDIS[Redis Memory Manager]
    FUP[Follow-up Conversation Engine]
end

GW --> ORCH
ORCH --> REDIS
ORCH --> FUP

%% ===================== A2A FABRIC =====================
subgraph FABRIC["A2A Agent Fabric"]
    AG1[Worker Agent A]
    AG2[Worker Agent B]
    AG3[Worker Agent C]
end

ORCH -->|A2A Protocol| AG1
ORCH -->|A2A Protocol| AG2
ORCH -->|A2A Protocol| AG3

%% ===================== HANDLER LAYER =====================
subgraph HANDLERS["Execution & Governance"]
    HITL[Human-in-the-Loop]
    CLEAN[Clean State Manager]
    AUTO[Auto Registration]
end

AG1 --> HITL
AG2 --> CLEAN
AG3 --> AUTO

%% ===================== MCP =====================
subgraph MCP_LAYER["MCP / Agent Registry"]
    MCP[MCP Server<br/>Discovery & Capability Index]
end

ORCH --> MCP
AG1 --> MCP
AG2 --> MCP
AG3 --> MCP

%% ===================== MEMORY & TRACE =====================
REDIS --> TRACE
AG1 --> REDIS
AG2 --> REDIS
AG3 --> REDIS